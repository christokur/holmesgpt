user_prompt: "Show me a graph of the memory usage for the kubernetes cluster over the last 30 minutes"
expected_output:
  - 'The answer should include something similar to << {{ tool_name: "...", random_key: "..." }} >>. Both tool_name and random_key MUST absolutely be present.'
evaluation:
  correctness: 2
generate_mocks: True
