apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-exporter-slow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-exporter-slow
  template:
    metadata:
      labels:
        app: analytics-exporter-slow
    spec:
      containers:
        - name: analytics-exporter-slow
          image: us-central1-docker.pkg.dev/genuine-flight-317411/devel/memory-eater:1.0
          imagePullPolicy: Always
          args:
            - 10Mi
            - "0"
            - 200Mi
            - "400"
            - "2"
          resources:
            limits:
              memory: 100Mi
            requests:
              memory: 100Mi
      restartPolicy: Always
      nodeSelector:
        kubernetes.io/arch: amd64
